/**
 * Expense surfaces (CONTRACT §3.5, §3.9) — the add / edit / detail write path.
 */

export { ExpenseEditorRoute } from './ExpenseEditorRoute'
export { ExpenseDetail } from './ExpenseDetail'
export { ExpenseForm, type ExpenseFormProps, splitTypeLabel } from './ExpenseForm'
