/** Activity feed surface (CONTRACT §3.10). */
export { ActivityFeed } from './ActivityFeed'
export { summarizeActivity, activityBucket, type ActivityLine, type ActivityBucket } from './summarize'
export { groupByDay, dayLabel, type DaySection } from './grouping'
