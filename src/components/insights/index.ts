/**
 * Insights component kit — the inline-SVG charts, stat cards, and presentation
 * metadata the group Insights surface (CONTRACT §7 A1) is built from.
 */

export { Donut, WeeklyBars, type DonutSegment, type DonutProps, type WeeklyBar, type WeeklyBarsProps } from './charts'
export { StatCard, DonutLegend, type StatCardProps, type DonutLegendProps } from './StatCard'
export { categoryLabel, categoryColor, CATEGORY_CHART_COLORS } from './meta'
