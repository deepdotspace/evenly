/**
 * Splitwise CSV import (`/app/import`, CONTRACT §3.13). Renders the 3-step wizard
 * inside the AppShell content column.
 */

import { ImportWizard } from '../../../components/import'

export default function ImportPage() {
  return <ImportWizard />
}
